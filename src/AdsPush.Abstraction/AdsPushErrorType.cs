namespace AdsPush.Abstraction
{
    public enum AdsPushErrorType
    {
        Unknown,
        InvalidToken,
        ServiceUnavailable,
        InvalidArgument,
        InvalidAuthConfiguration
    }
}
